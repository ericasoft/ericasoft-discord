{
  "name": "config",
  "description": "Configure the lobby and broadcast channels",
  "options": [
    {
      "name": "lobby",
      "description": "This is the channel that the users will be moved back to.",
      "type": 7,
      "required": true
    },
    {
      "name": "broadcast",
      "description": "This is the active broadcast channel that users will be moved to.",
      "type": 7,
      "required": true
    }
  ]
}